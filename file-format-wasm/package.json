{
  "name": "file-format-wasm",
  "version": "0.1.0",
  "description": "WebAssembly bindings for OmFileFormat",
  "type": "module",
  "main": "dist/om_reader_wasm.js",
  "files": [
    "dist/om_reader_wasm.js",
    "dist/om_reader_wasm.wasm"
  ],
  "scripts": {
    "build": "sudo docker run --rm -v $(pwd):/src emscripten/emsdk make -C /src",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "file-format",
    "wasm",
    "webassembly",
    "open-meteo"
  ],
  "license": "MIT"
}
