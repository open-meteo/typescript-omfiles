{
  "name": "omfiles",
  "private": true,
  "workspaces": [
    "file-format-wasm",
    "file-reader"
  ],
  "scripts": {
    "build:wasm": "cd file-format-wasm && npm run build",
    "build:js": "cd file-reader && npm run build",
    "build": "npm run build:wasm && npm run build:js",
    "test": "cd file-reader && npm test",
    "clean": "cd file-format-wasm && npm run clean && cd ../file-reader && rm -rf dist"
  },
  "devDependencies": {}
}
