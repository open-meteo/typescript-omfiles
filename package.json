{
  "name": "omfiles",
  "private": true,
  "workspaces": [
    "file-format-wasm",
    "omfiles-js"
  ],
  "scripts": {
    "build:wasm": "cd file-format-wasm && npm run build",
    "build:js": "cd omfiles-js && npm run build",
    "build": "npm run build:wasm && npm run build:js",
    "test": "cd omfiles-js && npm test",
    "clean": "cd file-format-wasm && npm run clean && cd ../omfiles-js && rm -rf dist"
  },
  "devDependencies": {}
}
