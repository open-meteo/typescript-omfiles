{
  "name": "omfiles",
  "private": true,
  "workspaces": [
    "omfiles-wasm",
    "omfiles-js"
  ],
  "scripts": {
    "build:wasm": "cd omfiles-wasm && npm run build",
    "build:js": "cd omfiles-js && npm run build",
    "build": "npm run build:wasm && npm run build:js",
    "test": "cd omfiles-js && npm test",
    "clean": "cd omfiles-wasm && npm run clean && cd ../omfiles-js && rm -rf dist"
  },
  "devDependencies": {}
}